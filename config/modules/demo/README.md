# `demo` 模块

一个用于验证模块化架构的模拟工具。

## 功能

- `demo`：运行主命令
- `demo -List`：列出功能
- `demo -Info`：显示模块信息

## 文件结构

- `demo.ps1`：主命令
- `helper.ps1`：辅助函数
- `config.json`：配置数据

## 说明

此模块仅用于测试 `modules/` + `tools/` 架构是否适合你的配置系统。